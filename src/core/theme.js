import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#1B50A7',
    secondary: '#622410',
    error: '#f13a59',
  },
};